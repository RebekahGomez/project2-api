import { Router } from "express";
const router = Router()
import express from "express";

import * as apiController from "./controller.js"

// all of these point to functions in controller.js
// remember to change the names below

// router.get("/", apiController.index)
// router.get("/:something", apiController.something)
// router.get("/:something2", apiController.something2)
// router.get("/:something3", apiController.something3)
// router.post("/", apiController.create)
// router.put("/:something4", apiController.edit)
// router.delete("/:something5", apiController.remove)

export default router